# WORK_LOG 2026-07-23

（本日誌記「立體字卡片（pop-up 鏤空字）」一條長弧：從機構誤讀彎路→使用者實體
折紙照片定案箱型→Phase 3 網頁模式（後端 popup.py＋/popup 頁＋/api/popup/svg＋
popup.html 前端）→缺字型測試 skip 修復＋run-based 連通標記後備。承 2026-07-15/18
的字模字型與鏤空字弧。雲端 Cowork 工作階段，沙箱 Playwright 實測、寫回本機、
收工檢查.bat 為最終閘門；功能已上 origin。commit `af861eb`，v0.14.263。）

---

## 機構弧：從「逐筆 tab」誤讀到「鏤空箱型」定案

使用者要「立體字卡片」——打字後生出可折立的鏤空字 pop-up 賀卡。我一開始照
**想像的機構**寫：把文字頂連接頂面的部分，拆成每個筆畫各一個小長方形 tab
（參考 DAD 立體卡範本），反覆多輪。使用者連續回「差異太大，根本不理解我的
修改」——這是明確的停損訊號，我卻又多繞了幾輪才真正回到原點。

**轉折**：使用者給了**實體折好的箱型照片**（「快樂」折成一個立體箱）。正確機構
其實最簡：

- **前立面**＝整片鏤空文字（字即結構、字身是材料、周圍透空剪掉）。
- **文字頂**＝一條摺線，往後折接**頂面 roof**。
- **文字底**＝一條摺線，往下折接**底座**。
- **字中一條中線谷折**穿過文字中段，卡片才能平整折合。
- 對稱：|字頂到中線|＝|字底到中線|（|a-d|=|b-c|），對稱才折得平。

**沒有 per-stroke tab**。我先前把「文字頂整條摺線」誤讀成「每個筆畫各一個 tab」，
把最簡的一條摺線複雜化成 N 個長方形——這是把紙上想像當成機構的典型錯。使用者
確認「這個鏤空箱型對了」後，雙層（兩箱疊成階梯、中線穿中間 tread 正中、上下兩排
等高對稱）也實體站得好，才進網頁模式。

**教訓**：紙藝／實體機構要**先折一個實體原型定案，再寫幾何**。程式碼描述的是
「摺起來會立起來」的實體行為，紙上想像最容易把簡單摺線複雜化；誤讀使用者修改時，
回到最簡機構（Occam）比堆疊特例更可能對。→ PRINCIPLES §68。

---

## Phase 3：網頁模式（打字→鏤空字 pop-up→SVG/PDF）

commit `af861eb`，v0.14.263。機構定案（單/雙層實體皆站得好）後實作網頁模式。

**後端 `exporters/popup.py`（新）**：

- 鏤空字前立面：`noto_hei`（思源黑體）多輪廓輪廓，`_outline_to_polylines`＋
  even-odd 填充規則光柵化（早期用單多邊形攤平版把「歡」畫成實心黑塊，改多輪廓
  even-odd 後正確鏤空）。
- **連筋（保證剪下不散）**：鏤空後會有浮件（如「口」「回」的中心塊），`_connect`
  批次把每個浮件補一條**最短縱向連筋橋**回主體，最多 6 輪、每輪只標記一次，直到
  整卡**單一連通（元件＝1）**。
- **連通標記**：`_label` 優先用 `scipy.ndimage.label`；缺 scipy 退回純 numpy
  **run-based union-find** `_label_runs`（早期純 numpy BFS 後備雙層要 24s，改
  run-based 降到 ~2.8s，並與 scipy 計數雙驗一致）。
- 折線分層輸出：`popup_to_svg` 描邊界迴圈成 CUT 黑實線＋折線（山折紅 #c62828／
  谷折藍 #1565c0／中線綠 #149046）；`build_popup(upper, lower="")` 回
  (card, folds, meta)，單層字straddle 中線、雙層 tread 正中對稱。

**Web 端點 `web/routes/pages.py`**：`GET /popup`（`_versioned_page` 服 popup.html）
＋`POST /api/popup/svg`（`PopupSvgRequest` 驗證：空字→422、>12 字→422；
`generate_popup`；`CharacterNotFound`→503 帶「思源黑體字型未安裝…執行
scripts/render_fetch_fonts.sh」指引）。PDF 沿用既有 `/api/card/pdf`（SVG→PDF）。

**前端 `web/static/popup.html`（新）**：上/下排文字輸入＋字高/roof/tread/字格寬
滑桿＋「產生預覽」POST→即時 SVG 預覽＋下載 SVG/PDF＋圖例＋狀態列（層數/尺寸/
連通/連筋數）。**自包含內嵌 JS、無子模組 import**——刻意避開 §67 的「半破快取」
缺口。

**pyproject**：web 選用相依 +`scipy>=1.10`（連通標記；缺則退回 numpy）；v0.14.263。

---

## 缺字型測試 skip 修復＋守門

首次跑收工檢查.bat 時 6 個 popup 測試 FAILED——`CharacterNotFound：思源黑體
未安裝`。

**根因**：`NotoSansTC-Bold.otf` **不進版控**、由 `scripts/render_fetch_fonts.sh`
於 Render 部署時抓；開發機（本裝置 `~/.stroke-order/hei-fonts/`）沒放就缺。這是
**部署期字型相依**，不是功能壞。

**修法**：需字型的測試沿用既有 `noto_hei` 那套守門——
`needs_hei = pytest.mark.skipif(not default_hei_font_path().exists())`，缺字型時
**skip 不是 fail**；不需字型的測試（`_label`/`_label_runs` 計數、頁面 200、空字/
超長 422）不掛守門獨立驗。端點缺字型回 **503 帶安裝指引**。再驗：有字型 8 過、
無字型 2 過 6 skip，零 failure。→ PRINCIPLES §68 姊妹律。

**路由快照**：刻意新增 `/popup`、`/api/popup/svg` 兩路由，同步更新
`test_route_snapshot.py` 的 ROUTE_SNAPSHOT（承既有「增刪路由先改快照」紀律）。

---

## 本輪事故與教訓（meta）

1. **停損訊號延遲反應**：使用者連兩次「差異太大、根本不理解」是明確的
   stop-and-replan 訊號，我卻又多繞幾輪才回到最簡機構。教訓：使用者說「不理解」
   時就該停手、要一張定案圖（實體照片），別再基於自己的想像疊修正。→ §68。
2. **橋接 `git status` 又留 `.git/index.lock`**：承 2026-07-22 教訓，device_bash
   不能刪檔、git 清鎖失敗→殘留鎖擋原生 `git add`。以 `mv` 改名移開；此後不再用
   device_bash 跑會鎖 index 的 git 指令。

---

## 收工總結

立體字卡片鏤空 pop-up：機構經**實體試折驗證**（單層＋雙層使用者實測站得好）、
Phase 3 網頁模式（`popup.py`＋`/popup`＋`/api/popup/svg`＋`popup.html`）、
全測試綠（有字型 popup 8 過；全庫 1855 passed／68 skipped、node 綜合綠、README
badge 自動更新 tests-1855／version-0.14.263），已上 origin `af861eb`。字型為部署
相依（Render 由 render_fetch_fonts.sh 抓；本機缺則測試 skip、端點 503）。
決策紀錄 `docs/decisions/2026-07-23_popup_hollow_box.md`；共通原則 PRINCIPLES §68。
