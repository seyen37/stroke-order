# Work Log — 2026-07-12 — 自訂字型全能力線＋注音欄（5cr→5cu）

**版本**：0.14.157 → 0.14.161
**Tests**：1642 → 1647（全綠收工）
**Commits**：3c9ff94 → aa99a55 共 7 筆，雙 remote 同步
**修改檔**：`exporters/grid.py`、`exporters/page.py`、`web/server.py`、
`web/static/index.html`、`doodle_engine.js`、`doodle_worker.js`、
`收工檢查.bat`、`REF_ANALYSIS_WORKSHEET_ECOSYSTEM.md`、測試×4

---

## 一、本日 narrative

1. **開工健檢**：沙箱掛載雙截斷（pyproject＋server.py）——照 §8.8
   git archive 乾淨副本＋PYTHONPATH 起 server，三路由 200。
   **Render 5cq 驗收 ✓**：/vendor/status 部署即雙 cached:true，
   執行期零外網依賴達成。
2. **5cr 凍頁陷阱關閉**：使用者以本機伺服器（127.0.0.1）重測
   OpenCV 整頁凍死——證實執行病灶與檔案來源徹底無關（環境層
   定性再獲獨立證據），並暴露降級階梯真 bug：worker 被看門狗
   砍掉後退「主執行緒直跑」，但主執行緒懸掛時 **timer 全停、
   看門狗無效＝整頁凍死**。修：opencv 引擎 worker 失敗直接
   throw 給 UI 退伺服器＋失敗記憶；browser 引擎維持原 fallback。
3. **5cs 自訂字型機器軌跡**：fontCharTracks（canvas 384² 光柵化
   → 復用 5cg 三件組 → EM 2048 軌跡、per-char 快取）＋
   gridUserFontGcode（復刻 render_grid_gcode 全慣例）；G-code 鈕
   前端 blob 復活；「非教育部筆順」誠實標注。node 假 DOM 座標級
   驗證（flip_y／格位移／ghost 跳過）全對。
4. **5ct 注入擴筆記／信紙**：發現兩模式共用 page.py `_char_svg`
   且字元群組 transform 自帶 mm 縮放——**一處加錨點、多模式
   生效，5cn 注入器一行不改直接復用**。三模式共用一次字型載入。
5. **5cu 注音欄（REF 行動項之首）**：前端 pinyin-pro＋
   pinyinToZhuyin 確定性規則表（33 樣本 node 驗證）→ zhuyin_map
   參數（伺服器零字典依賴）；grid.py pair 版面（2:1 沿稿紙慣例）
   ＋ _zhuyin_strip 以 _cell_content 渲染符號（描紅樣式全繼承）
   ＋聲調 ˊˇˋ˙ 手作 polyline；data-pair-em 讓 5cs G-code 格距
   同步。**「注音也能練筆順」——全字帖工具生態獨家**。
6. **REF 增補（使用者第二批資源）**：注音 IVS 字型生態（字嗨
   注音體／讀音選擇工具／McBopomofo／ToneOZ）。關鍵發現：
   ⚠ pinyin-pro 是大陸讀音體系，與台灣教育部一字多音審定表
   有落差——「台灣讀音校正表」列行動項。立即組合技：字嗨注音體
   TTF 餵 5cn 自訂字型＋5cu 注音欄同紙互補。
7. **chore**：收工檢查.bat 寫死「0.14.134」提醒改通用句
   （badge 抄實跑，§9.10 烙進腳本）——老待辦清償。

## 二、本日終帳

| 輪 | 主題 | 一句話 |
|---|---|---|
| 5cr | 凍頁陷阱 | fallback 目的地與失敗層共享病灶＝看門狗失效；opencv 不退主執行緒 |
| 5cs | 字型機器軌跡 | 光柵化＋5cg 三件組復用；G-code 慣例逐項對齊；node 座標級驗證 |
| 5ct | 擴模式 | page.py 一處錨點、注入器零改——分層紅利在第二個消費者兌現 |
| 5cu | 注音欄 | 前端管語言（規則表）、伺服器管排版（pair 版面）；注音描紅獨家 |
| REF | IVS 生態 | 破音字社群共識解；讀音兩套標準警覺；印刷注音×筆順注音組合技 |

**自訂字型能力線收束**：字帖（描紅＋G-code）＋筆記＋信紙
（描紅），三模式共用載入；**注音線開張**：字帖注音欄 v1。

**本日教訓（已入 PRINCIPLES §10）**：分層紅利、語言知識在前端、
fallback 病灶隔離、node 假 DOM 座標驗證、規則表優於窮舉＋讀音
標準警覺、生態參考轉組合技。

## 三、Followups

- [ ] 注音 v2：破音字修正 UI（polyphonic 下拉）、台灣讀音校正表、
      注音符號進 G-code（有筆順資料，機器可寫）、筆記/信紙注音欄
- [ ] 筆記/信紙 userfont G-code（page 型 mm 座標版組裝）
- [ ] 家用機 OpenCV 引擎驗證（環境病灶應不存在）
- [ ] 5cu/5cs/5ct 部署後實機驗收（注音欄描紅頁＋G-code 模擬器看軌跡）
