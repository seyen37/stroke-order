# Work Log — 2026-07-11 — Phase 5bo 科普教育分類（6 preset + 3 table 版面頁）

**版本**：0.14.133 → 0.14.134
**修改檔**：`sutras.py`、`web/server.py`、`web/static/index.html`、
`exporters/periodic_table.py`（新）、`exporters/multiplication_table.py`（新）、
`exporters/solar_terms.py`（新）、`tests/test_sutra.py`、
`tests/test_periodic_table.py`（新）、`tests/test_multiplication_table.py`（新）、
`tests/test_solar_terms.py`（新）、`samples/5bo/`（新，6 txt + README）、
`REF_ANALYSIS_VECTORLINE.md`（新）
**Tests**：1511 → 預計 1537（+2 sutra、+11 週期表、+7 乘法表、+6 節氣；以本機全跑為準）

---

## 一、本日 narrative

1. **開工**：沙箱起 server 三路由驗證；本機新增 `啟動伺服器.bat` 供雙擊啟動。
   SOP-0 fetch-first 沙箱無 SSH 金鑰跑不了，待 user host 補跑。
2. **VectorLine 參考分析**：Studio0808 的圖片→雷切線稿工具（OpenCV.js WASM），
   寫入 `REF_ANALYSIS_VECTORLINE.md`。可借鑑：分層 DXF、SVG mm 實體尺寸
   audit、塗鴉模式前端化樣板。邊界：無筆順語意。
3. **Phase 5bo 主體**（三輪漸進）：
   - 週期表 preset + 第 8 分類「科普教育」+ `page_type=table` 新頁型
     （18 族×7 週期＋鑭錒系抽離、分類淡色底、原子序＋符號＋描紅）。
     118 字含 104-118 罕見字（CNS fallback）逐字驗證全過。
   - 純文字三部：國字大寫數字（19）、十二時辰十二生肖（24）、太陽系天體（25）。
   - 九九乘法表（口訣 `mnemonic()` 程式化生成，45 句逐句測試）＋
     二十四節氣（4 季×6 節氣、季節色帶、國曆日期提示）。
     table 頁型從 if-chain 泛化成 `_table_page_renderer()` registry。

## 二、本日學到的規則

### CJK 一律描邊、`<text>` 只放 ASCII（再度驗證）
節氣日期提示原寫「約 2/4」，「約」在無 CJK 字型主機（cairosvg → PDF）變
豆腐字。與 5bv mark_renderer="polyline" 同源教訓：凡會經過 server-side
rasterise 的 CJK，一律走 char_loader 描邊；`<text>` 保留給數字與拉丁。

### 慢渲染 API 測試 → stub loader，正確性由 exporter 層扛
全頁 118 字真實渲染 40-60s，不入測試。API 測試 monkeypatch `_load` 驗
路由與組版；glyph 正確性由 exporter 單元測試＋視覺驗證覆蓋。乘法表／節氣
只有 11／25 個相異字，exporter 內建 memo loader，真實渲染 0-4s。

### 協作沙箱掛載同步會「前半新後半舊」
Windows 側 Edit 後沙箱 mtime/size 不更新 → Python 沿用舊 .pyc、檔案讀取
被舊長度截斷。解法：`git archive HEAD` 建乾淨副本＋程式化重套修改再驗證；
Windows 側檔案為權威版本。已記入協作 memory。

## 三、Followups

- [ ] 本機 `收工檢查.bat`：全量 pytest 確認實際測試數 → README tests badge
      與 version badge 對齊（0.14.134）
- [ ] 6 個 txt 部署：本機與 Render 的 sutra 目錄補檔（samples/5bo/ 為源）
- [ ] 科普教育下一批候選：康熙 214 部首（與組件化願景直接呼應）、倉頡字根
- [ ] VectorLine 借鑑項排優先序（分層 DXF / mm 尺寸 audit）
